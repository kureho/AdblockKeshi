import SwiftUI

struct ContentView: View {
    var body: some View {
        VStack {
            Text("広告消し")
                .font(.largeTitle)
                .padding()
            Text("scaffold OK")
                .foregroundColor(.secondary)
        }
    }
}

#Preview {
    ContentView()
}
